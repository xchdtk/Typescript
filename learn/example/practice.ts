// 1_type-basic.ts
const str2: string = 'hello';

const num2: number = 10;

const arr2:Array<string> = [];

const items2: string[] = [];

const address2: [string, number] = ['판교', 40];

const obj2: object = {};

const person2: { age:number, name: string } = { age: 100, name: 'capt'};

let isLogin2: boolean = false;

// 2_function.ts
function sum3(a: number, b: number) {
    return a + b;
}

function add2(): number {
    return 10;
}

